<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Menucraft{% endblock %}</title>

    {# Bootstrap CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    {# Styles personnalisés #}
    <style>
        .navbar-brand {
            font-size: 24px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: #ff5722; /* Couleur du logo */
        }
        .navbar-brand:hover {
            color: #e64a19;
        }
        footer {
            background-color: #f8f9fa;
            padding: 20px 0;
            text-align: center;
            margin-top: 30px;
        }
    </style>

    {% block stylesheets %}{% endblock %}
</head>
<body>

{# Barre de navigation #}
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="{{ path('menucraft_homepage') }}">
            Menucraft
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link" href="{{ path('menucraft_homepage') }}">Accueil</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('menucraft_recipe_list') }}">Recettes</a></li>
            </ul>
        </div>
    </div>
</nav>

{# Contenu de la page #}
<div class="container mt-4">
    {% block body %}{% endblock %}
</div>

{# Pied de page #}
<footer>
    <div class="container">
        <p class="mb-0">© {{ "now"|date("Y") }} Menucraft - Tous droits réservés.</p>
    </div>
</footer>

{# Bootstrap JS #}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://kit.fontawesome.com/1497a6bbb1.js" crossorigin="anonymous"></script>

{% block javascripts %}{% endblock %}
</body>
</html>
